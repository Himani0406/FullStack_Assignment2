{
  "name": "assignment-2",
  "version": "1.0.0",
  "description": "Assignment 2",
  "main": "server.js",
  "scripts": {
    "start": "node server//server.js",
    "compile": "babel src --presets react --out-dir public",
    "watch": "babel src --presets react --out-dir public --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Himani",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  }
}
